# 与risc_v实现不同的是，这里的skernel生成不能简单的make，需要进入/arceos/payload/skernel_aarch64 中

make build

# 使用 update_disk.sh 脚本将 skernel 写入磁盘

./update_disk.sh ./target/aarch64-unknown-none/release/skernel_aarch64.bin

# 使用 BLK=y 启用块设备，PFLASH=n 禁用 pflash

make run A=tour/h_1_1 ARCH=aarch64 BLK=y PFLASH=n