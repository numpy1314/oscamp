## 创建 pflash 镜像
make pflash_img ARCH=aarch64

## 创建磁盘镜像
make disk_img

## 编译 skernel_aarch64
make payload

## 将 skernel_aarch64 写入磁盘
./update_disk.sh ./payload/skernel_aarch64/skernel_aarch64

## 编译并运行
make run A=tour/h_1_1 ARCH=aarch64 BLK=y PLATFORM=aarch64-qemu-virt-hv