# 虚拟化开发学习日志

## 2025-11-16

### 问题1：MMIO 和 VirtIO 有什么区别？

#### MMIO (Memory-Mapped I/O)

**MMIO** 是一种**硬件访问机制**：
- 将设备的控制寄存器映射到内存地址空间
- CPU 通过普通的内存读写指令（load/store）来访问设备
- 例如：向地址 `0x10000000` 写入数据，实际上是在控制某个硬件设备

#### VirtIO

**VirtIO** 是一种**虚拟化 I/O 标准协议**：
- 定义了虚拟机和 Hypervisor 之间通信的接口规范
- 包括虚拟网卡、虚拟磁盘、虚拟GPU等
- 通过 virtqueue（虚拟队列）进行高效的数据传输

#### 两者的关系

**VirtIO 设备可以通过 MMIO 方式访问**！

```
VirtIO-MMIO = VirtIO协议 + MMIO访问方式
```

- **VirtIO-MMIO**：通过内存映射的方式访问 VirtIO 设备（常用于 ARM/RISC-V）
- **VirtIO-PCI**：通过 PCI 总线访问 VirtIO 设备（常用于 x86）

#### 在 aarch64-qemu-virt.toml 配置文件中的体现

**mmio-regions**（所有需要 MMIO 访问的设备）：
```toml
mmio-regions = [
    ["0x0900_0000", "0x1000"],      # PL011 UART (串口)
    ["0x0910_0000", "0x1000"],      # PL031 RTC (时钟)
    ["0x0800_0000", "0x2_0000"],    # GICv2 (中断控制器)
    ["0x0a00_0000", "0x4000"],      # VirtIO 区域
    ["0x1000_0000", "0x2eff_0000"], # PCI 内存
    ["0x40_1000_0000", "0x1000_0000"], # PCI 配置空间
]
```

**virtio-mmio-regions**（专门给 VirtIO 设备使用的 MMIO 地址）：
```toml
virtio-mmio-regions = [
    ["0x0a00_0000", "0x200"],  # VirtIO设备0
    ["0x0a00_0200", "0x200"],  # VirtIO设备1
    ["0x0a00_0400", "0x200"],  # VirtIO设备2
    ...
]
```

#### 层次关系图

```
┌─────────────────────────────────────┐
│      所有 MMIO 设备 (mmio-regions)    │
│  ┌───────────────────────────────┐  │
│  │   UART (0x0900_0000)          │  │ ← 传统硬件
│  ├───────────────────────────────┤  │
│  │   RTC  (0x0910_0000)          │  │ ← 传统硬件
│  ├───────────────────────────────┤  │
│  │   GIC  (0x0800_0000)          │  │ ← 中断控制器
│  ├───────────────────────────────┤  │
│  │ ┌─────────────────────────────┴──┼── VirtIO区域(0x0a00_0000)
│  │ │  VirtIO-MMIO 设备们         │  │
│  │ │  (virtio-mmio-regions)      │  │ ← VirtIO协议 + MMIO访问
│  │ │  - 虚拟网卡                 │  │
│  │ │  - 虚拟磁盘                 │  │
│  │ │  - 虚拟输入设备             │  │
│  │ └─────────────────────────────┘  │
│  └───────────────────────────────────┘
└─────────────────────────────────────┘
```

#### 总结

- **MMIO** = 访问方式（通过内存地址读写）
- **VirtIO** = 设备类型（虚拟化 I/O 设备的标准）
- **VirtIO-MMIO** = VirtIO设备使用MMIO方式访问

---

### 问题3：AArch64 中定时器管理和串口调试应该如何实现？

#### 1. 定时器管理 (Generic Timer)

AArch64 使用**通用定时器**，通过**系统寄存器**直接访问，不需要 PSCI。

**关键系统寄存器**：
```rust
CNTFRQ_EL0   // Counter Frequency (定时器频率)
CNTPCT_EL0   // Counter Physical Count (当前计数值)
CNTP_CTL_EL0 // Counter Physical Timer Control (定时器控制)
CNTP_TVAL_EL0 // Counter Physical Timer Value (定时器超时值)
```

**ArceOS 中的实现** (`generic_timer.rs`)：

**（1）读取当前时间（ticks）**
```rust
pub fn current_ticks() -> u64 {
    CNTPCT_EL0.get()  // 直接读取系统寄存器
}
```

**（2）设置定时器中断**
```rust
pub fn set_oneshot_timer(deadline_ns: u64) {
    let cnptct = CNTPCT_EL0.get();           // 当前时间
    let cnptct_deadline = nanos_to_ticks(deadline_ns); // 目标时间
    if cnptct < cnptct_deadline {
        let interval = cnptct_deadline - cnptct;
        CNTP_TVAL_EL0.set(interval);  // 设置超时间隔
    } else {
        CNTP_TVAL_EL0.set(0);         // 立即触发
    }
}
```

**（3）初始化定时器**
```rust
pub(crate) fn init_percpu() {
    // 启用定时器
    CNTP_CTL_EL0.write(CNTP_CTL_EL0::ENABLE::SET);
    CNTP_TVAL_EL0.set(0);
    // 使能定时器中断
    crate::platform::irq::set_enable(TIMER_IRQ_NUM, true);
}
```

**定时器的工作原理**：
```
1. 硬件自动递增计数器 CNTPCT_EL0
2. 当 CNTPCT_EL0 >= (当前值 + CNTP_TVAL_EL0) 时
3. 触发定时器中断
4. CPU 跳转到中断处理程序
```

#### 2. 串口调试 (PL011 UART)

AArch64 QEMU 虚拟机使用 **PL011 UART** 控制器，通过 **MMIO** 访问。

**PL011 UART 的实现** (`pl011.rs`)：

**（1）UART 寄存器映射**
```rust
const UART_BASE: PhysAddr = pa!(0x0900_0000);  // 来自配置文件

static UART: SpinNoIrq<Pl011Uart> = 
    SpinNoIrq::new(Pl011Uart::new(
        phys_to_virt(UART_BASE).as_mut_ptr()
    ));
```

**（2）输出字符（调试打印）**
```rust
pub fn putchar(c: u8) {
    let mut uart = UART.lock();
    match c {
        b'\n' => {
            uart.putchar(b'\r');  // 换行需要先发送回车
            uart.putchar(b'\n');
        }
        c => uart.putchar(c),
    }
}
```

**（3）读取字符（用户输入）**
```rust
pub fn getchar() -> Option<u8> {
    UART.lock().getchar()
}
```

**（4）UART 中断处理**
```rust
pub fn handle() {
    let is_receive_interrupt = UART.lock().is_receive_interrupt();
    UART.lock().ack_interrupts();
    if is_receive_interrupt {
        while let Some(c) = getchar() {
            putchar(c);  // 回显字符
        }
    }
}
```

#### 3. 对比 RISC-V 和 AArch64

| 功能 | RISC-V | AArch64 |
|------|--------|------|
| **定时器** | 通过 SBI 调用 `set_timer()` | 直接访问系统寄存器 `CNTP_TVAL_EL0` |
| **串口** | 通过 SBI 调用 `console_putchar()` | 通过 MMIO 访问 PL011 寄存器 |
| **访问方式** | `ecall` 陷入 M-mode | 直接读写内存/寄存器 |
| **性能** | 较慢（需要特权级切换） | 较快（直接访问） |

#### 4. 在虚拟化场景中的应用

如果要在虚拟化场景中为 Guest 提供定时器和串口支持，需要：

**定时器虚拟化示例**：
```rust
// 在 sysregs.rs 中添加定时器寄存器
pub struct CntpCtlEl0;
impl AArch64SysRegTrait for CntpCtlEl0 {
    fn read(&self) -> usize {
        let val: usize;
        unsafe {
            core::arch::asm!("mrs {}, cntp_ctl_el0", out(reg) val);
        }
        val
    }
    fn write(&self, val: usize) {
        unsafe {
            core::arch::asm!("msr cntp_ctl_el0, {}", in(reg) val);
        }
    }
}
```

**串口通过 MMIO 陷入处理**：
```rust
// 当 Guest 访问 UART_BASE (0x0900_0000) 时
// 会触发 Data Abort 异常
// 在异常处理中模拟 UART 行为
match fault_addr {
    0x0900_0000 => {
        // 模拟 PL011 UART 读写
        handle_uart_access(vcpu, is_write, data);
    }
    _ => {}
}
```

#### 总结

相比 RISC-V 的 SBI，AArch64 更多地依赖：
- **直接硬件访问**（定时器通过系统寄存器）
- **MMIO**（串口通过内存映射）

这反映了两个架构设计哲学的不同：
- **RISC-V**：通过固件接口（SBI）提供统一抽象
- **AArch64**：提供丰富的硬件机制，直接访问

---

### 问题2：RISC-V 中的 SBI 和 AArch64 中的 PSCI 有什么区别？

#### SBI (Supervisor Binary Interface) - RISC-V

**SBI** 是 RISC-V 架构中的**固件接口标准**：

**特点**：
- **层级**：运行在 M-mode（Machine Mode，最高权限）
- **服务对象**：为 S-mode（Supervisor Mode）的操作系统提供服务
- **功能**：
  - 定时器管理
  - IPI（处理器间中断）
  - 系统复位/关机
  - Hart（硬件线程）启动/停止
  - 串口输出（调试用）

**调用方式**：
```rust
// RISC-V 中通过 ecall 指令调用 SBI
ecall  // 从 S-mode 陷入 M-mode
```

#### PSCI (Power State Coordination Interface) - AArch64

**PSCI** 是 ARM 架构中的**电源状态协调接口**：

**特点**：
- **层级**：运行在 EL3（Exception Level 3，最高权限）或 EL2（Hypervisor）
- **服务对象**：为 EL1（操作系统）或 EL2（Hypervisor）提供服务
- **功能**：
  - CPU 上电/下电
  - CPU 挂起/恢复
  - 系统复位/关机
  - CPU 亲和性管理
  - 多核启动

**调用方式**：
```rust
// AArch64 中通过 HVC 或 SMC 指令调用 PSCI
hvc #0  // Hypervisor Call (EL2)
smc #0  // Secure Monitor Call (EL3)
```

#### 对比表格

| 特性 | SBI (RISC-V) | PSCI (AArch64) |
|------|--------------|----------------|
| **运行层级** | M-mode | EL3 或 EL2 |
| **调用指令** | `ecall` | `smc` / `hvc` |
| **主要功能** | 固件服务接口 | 电源状态管理 |
| **定时器管理** |   是 | ❌ 否（通过其他机制） |
| **CPU管理** |   Hart 管理 |   CPU 管理 |
| **系统控制** |   复位/关机 |   复位/关机 |
| **串口调试** |   有 | ❌ 无 |
| **标准化** | SBI 规范 | ARM PSCI 规范 |

#### 在 h_1_1 项目中的 PSCI 实现

**配置文件中指定 PSCI 调用方式**（`aarch64-qemu-virt.toml`）：
```toml
# PSCI
psci-method = "hvc"  # 使用 HVC 指令调用 PSCI
```

**PSCI 功能实现**（`h_1_1/src/psci/mod.rs`）：
```rust
// PSCI 功能 ID
0x84000000 => Version         // 查询 PSCI 版本
0x84000002 => CpuOff          // 关闭 CPU
0x84000003 => CpuOn           // 启动 CPU
0x84000008 => SystemOff       // 系统关机
0x84000009 => SystemReset     // 系统重启
```

**PSCI 错误码定义**：
```rust
PSCI_SUCCESS              =  0   // 成功
PSCI_ERR_NOT_SUPPORTED    = -1   // 不支持
PSCI_ERR_INVALID_PARAMS   = -2   // 参数无效
PSCI_ERR_ALREADY_ON       = -4   // CPU 已开启
PSCI_ERR_ON_PENDING       = -5   // CPU 启动中
PSCI_ERR_INTERNAL_FAILURE = -6   // 内部错误
PSCI_ERR_NOT_PRESENT      = -7   // CPU 不存在
PSCI_ERR_DISABLED         = -8   // 功能已禁用
PSCI_ERR_INVALID_ADDRESS  = -9   // 地址无效
```

#### 架构对比图

```
RISC-V 虚拟化                    AArch64 虚拟化
═══════════════                  ═══════════════
┌─────────────┐                  ┌─────────────┐
│   M-mode    │ ← SBI 服务       │     EL3     │ ← PSCI 服务
│  (OpenSBI)  │                  │  (ATF/QEMU) │
└─────────────┘                  └─────────────┘
      ↑ ecall                          ↑ hvc/smc
┌─────────────┐                  ┌─────────────┐
│   S-mode    │                  │     EL2     │
│     OS      │                  │ Hypervisor  │
└─────────────┘                  └─────────────┘
```

#### 核心区别总结

1. **设计理念**：
   - **SBI**：更通用的固件接口，包含更多基础服务（如定时器、串口）
   - **PSCI**：专注于电源和 CPU 状态管理

2. **调用成本**：
   - **SBI**：`ecall` 指令，相对简单
   - **PSCI**：`hvc`/`smc` 指令，可能涉及更多的状态切换

3. **使用场景**：
   - **SBI**：RISC-V 虚拟化中必不可少，提供底层固件服务
   - **PSCI**：AArch64 虚拟化中专门处理多核和电源管理

---

### 问题4：为什么 RISC-V 虚拟化启动时会有 OpenSBI 标识？

#### 1. RISC-V 特权级架构

RISC-V 采用分层特权级设计：

```
┌─────────────────────────────────────┐
│   M-mode (Machine Mode)             │ ← OpenSBI 运行在这里
│   - 最高特权级                       │
│   - 提供 SBI 接口                    │
│   - 硬件初始化                       │
└─────────────┬───────────────────────┘
              │ SBI 调用 (ecall)
┌─────────────▼───────────────────────┐
│   HS-mode (Hypervisor-Supervisor)   │ ← h_1_0 Hypervisor 运行在这里
│   - ArceOS + h_1_0                  │
│   - 虚拟化支持                       │
└─────────────┬───────────────────────┘
              │ 虚拟化
┌─────────────▼───────────────────────┐
│   VS-mode (Virtual Supervisor)      │ ← Guest OS 运行在这里
│   - Guest 虚拟机                     │
└─────────────────────────────────────┘
```

#### 2. QEMU 启动流程

```bash
qemu-system-riscv64 -bios default ...
```

`-bios default` 告诉 QEMU 使用**内置的 OpenSBI 固件**：

```
1. QEMU 启动
   ↓
2. OpenSBI (M-mode) 初始化
   - 初始化硬件
   - 设置 SBI 服务
   - 打印启动信息（OpenSBI banner）
   ↓
3. OpenSBI 加载并跳转到 kernel
   - 从 -kernel 参数指定的地址加载
   - 切换到 S-mode/HS-mode
   - 跳转到 0x80200000 (ArceOS/h_1_0)
   ↓
4. h_1_0 Hypervisor 开始运行
   - 打印 "ArceOS" banner
   - 打印 "Hypervisor ..."
```

#### 3. OpenSBI 的作用

OpenSBI 为 Hypervisor 提供底层 SBI 服务：

```rust
// 当代码调用 SBI 时，实际上是调用 OpenSBI
sbi_rt::system_reset(...)  // 系统重启
sbi_rt::set_timer(...)     // 设置定时器
sbi_rt::send_ipi(...)      // 发送核间中断
```

#### 4. 启动日志解析

```
OpenSBI v1.0              ← OpenSBI 固件启动
...
Boot HART ID: 0           ← 硬件线程 0 启动
Boot HART ISA: rv64...    ← RISC-V 64位指令集
...
Domain0 Next Address: 0x80200000  ← 将跳转到这个地址（kernel）
Domain0 Next Mode: S-mode         ← 以 S-mode 运行

ArceOS                    ← h_1_0 开始运行
...
Hypervisor ...            ← h_1_0 主程序
```

#### 5. RISC-V vs AArch64 对比

| 架构 | 固件层 | Hypervisor 层 |
|------|--------|-------------|
| **RISC-V** | OpenSBI (M-mode) | ArceOS (HS-mode) |
| **AArch64** | QEMU 内置固件 (EL3) | ArceOS (EL2) |

**关键区别**：
- **RISC-V**：必须有 M-mode 固件（OpenSBI），通过 SBI 提供服务
- **AArch64**：QEMU 可以直接启动到 EL2，不需要显式固件层

#### 6. 为什么需要 OpenSBI？

RISC-V 的设计哲学：**硬件简单，软件灵活**

- M-mode 处理最底层的硬件初始化
- S-mode/HS-mode 不直接访问硬件
- 通过 **SBI (Supervisor Binary Interface)** 统一接口访问底层功能
- 实现了硬件抽象，不同的 RISC-V 硬件可以使用相同的 SBI 接口

#### 总结

OpenSBI 是 RISC-V 虚拟化**必需的底层固件**，它：
1. 初始化硬件
2. 提供 SBI 服务接口
3. 加载并启动 Hypervisor
4. 处理底层的硬件操作

这不是 bug，而是 RISC-V 架构的**设计特性**！OpenSBI 相当于 RISC-V 的 "BIOS"，为上层软件提供统一的硬件抽象层。

---

### 问题5：AArch64 是如何进入 EL2 的？是通过 PSCI 吗？

#### 答案：不是！

AArch64 虚拟化**不是通过 PSCI 进入 EL2 的**。PSCI 是运行时的 CPU 管理接口，不是启动机制。

#### 1. QEMU 启动方式

QEMU 通过命令行参数直接启动到 EL2：

```bash
qemu-system-aarch64 \
  -machine virt,virtualization=on \  # 关键参数！
  -kernel $(OUT_BIN)
```

**`virtualization=on` 的作用**：
- 告诉 QEMU 直接将 CPU 初始化为 **EL2 (Hypervisor) 模式**
- 不需要固件层
- 直接跳转到 kernel 入口地址

#### 2. 启动流程对比

**RISC-V 启动流程**（需要固件）：
```
1. QEMU 启动
   ↓
2. OpenSBI (M-mode) 初始化  ← 必须有固件层
   ↓
3. 跳转到 S-mode/HS-mode
   ↓
4. ArceOS Hypervisor 运行
```

**AArch64 启动流程**（无需固件）：
```
1. QEMU 启动
   ↓
2. 直接进入 EL2 (Hypervisor)  ← QEMU 模拟器功能
   ↓
3. ArceOS Hypervisor 运行
```

#### 3. ArceOS 的启动代码

在 `boot.rs` 中，有两个关键函数：

**（1）switch_to_el1() - 普通 OS 使用**
```rust
unsafe fn switch_to_el1() {
    let current_el = CurrentEL.read(CurrentEL::EL);
    if current_el >= 2 {
        // 从 EL2 降到 EL1
        SPSR_EL2.write(SPSR_EL2::M::EL1h ...);
        ELR_EL2.set(LR.get());
        asm::eret();  // 返回到 EL1
    }
}
```

**（2）stay_in_el2() - Hypervisor 使用**
```rust
unsafe fn stay_in_el2() {
    let current_el = CurrentEL.read(CurrentEL::EL);
    
    if current_el == 3 {
        // 如果从 EL3 启动，降到 EL2
        SPSR_EL3.write(SPSR_EL3::M::EL2h ...);
        ELR_EL3.set(LR.get());
        asm::eret();  // 返回到 EL2
    }
    
    // 如果已经在 EL2，直接配置环境
    if current_el >= 2 {
        // 配置 HCR_EL2（Hypervisor 配置寄存器）
        // 配置 VBAR_EL2（异常向量基地址）
        // 配置定时器等
    }
}
```

#### 4. 真实硬件上的 AArch64 启动

在真实硬件（如树莓派 4）上，启动流程更复杂：

```
1. Boot ROM（固化在芯片中）
   ↓
2. Bootloader (U-Boot/UEFI)
   ↓
3. ARM Trusted Firmware (ATF) - 运行在 EL3
   ↓
4. ATF 可能使用 PSCI 启动其他 CPU 核心
   ↓
5. ATF 将 CPU 切换到 EL2 或 EL1
   ↓
6. 跳转到 Hypervisor/OS 入口
```

#### 5. PSCI 的真正用途

PSCI **不是启动机制**，而是**运行时 API**：

```rust
// PSCI 的实际用途：

// 1. 多核心管理
PSCI::cpu_on(cpu_id, entry_point, context)  // 启动其他 CPU 核心
PSCI::cpu_off()                              // 关闭当前 CPU

// 2. 电源管理
PSCI::cpu_suspend(power_state)               // 挂起 CPU

// 3. 系统控制
PSCI::system_reset()                         // 系统重启
PSCI::system_off()                           // 系统关机
```

在虚拟化场景中，PSCI 用于：
- **Guest VM 管理自己的虚拟 CPU**
- **Hypervisor 拦截并模拟 PSCI 调用**

#### 6. 对比表格

| 特性 | RISC-V | AArch64 |
|------|--------|----------|
| **启动方式** | OpenSBI 固件 | QEMU 直接进入 |
| **固件层** | 必需 (M-mode) | 可选 (EL3 ATF) |
| **QEMU 参数** | `-bios default` | `-machine virtualization=on` |
| **启动特权级** | HS-mode | EL2 |
| **固件接口** | SBI (必需) | PSCI (可选) |
| **启动复杂度** | 需要固件初始化 | 可直接启动 |

#### 7. 代码中的体现

查看 `qemu.mk` 配置：

**RISC-V**:
```makefile
qemu_args-riscv64 := \
  -machine virt \
  -bios default \      # 加载 OpenSBI 固件
  -kernel $(OUT_BIN)
```

**AArch64**:
```makefile
qemu_args-aarch64 := \
  -cpu max \
  -machine virt,virtualization=on \  # 直接启用虚拟化
  -kernel $(OUT_BIN)
```

#### 总结

**AArch64 进入 EL2 的方式**：

1. **QEMU 模拟**：通过 `virtualization=on` 参数直接启动到 EL2
2. **真实硬件**：通过 Bootloader/ATF 设置 CPU 状态后跳转到 EL2
3. **ArceOS 代码**：`stay_in_el2()` 确保运行在 EL2 并配置环境

**PSCI 的作用**：
- ❌ **不是**启动机制
-   **是**运行时的 CPU 和电源管理 API
-   用于多核启动、电源管理、系统重启等

**核心区别**：
- **RISC-V**：启动依赖固件 (OpenSBI)，通过 SBI 提供服务
- **AArch64**：可以无固件启动，PSCI 是可选的运行时服务

这就是为什么 AArch64 虚拟化更加灵活 —— **硬件直接支持 Hypervisor 模式**，不强制要求固件层！

### 问题6：在 AArch64 复现中为什么不需要 pflash？

#### 核心原因
- QEMU 通过 `-machine virt,virtualization=on` 可直接将 CPU 置于 EL2（Hypervisor）运行，无需固件镜像。
- 当前实验流程通过文件系统加载 Guest：`loader::load_vm_image` 读取 `/sbin/skernel` 并拷贝到 `VM_ENTRY`，因此需要块设备磁盘 `disk.img`（FAT32 + VirtIO‑Blk），而非固件闪存。
- `pflash.img` 是“平台闪存”（类似 ROM），用于固件/引导数据，并不是文件系统，不能满足 `File::open` 打开 `/sbin/skernel` 的需求。

#### 何时才需要 pflash
- 若改造为“从固定物理地址直接加载镜像”，可将镜像放入 pflash 映射区，Hypervisor 改用内存拷贝而非文件 API（替换 `loader::load_vm_image` 的文件读取逻辑）。当前实验未采用该路径。

#### 正确的运行方式（当前实验）
- 将 Guest 写入磁盘：`./arceos/update_disk.sh /路径/到/skernel`
- 运行参数：`PFLASH=n BLK=y`（启用块设备、禁用 pflash）


---

## 2025-11-17

### h_1_1 AArch64 虚拟化实验调试记录

#### 实验目标
实现 AArch64 架构下的基本虚拟化功能，能够在 EL2 (Hypervisor) 模式下运行 Guest OS，正确处理 Guest 的上下文切换。

---

### 调试问题汇总

#### 问题 1: 寄存器冲突导致 ELR_EL2 被错误覆盖

**错误现象**：
```
Unhandled trap: EC=0x20, ESR_EL2=0x82000005, ELR_EL2=0x3c5
```

**错误分析**：
- `ELR_EL2 = 0x3c5`：这是 SPSR_EL2 的值，不是 Guest 入口地址！
- `EC = 0x20`：Exception Class = 32，表示 Instruction Abort from Lower EL
- `ESR_EL2 = 0x82000005`：
  - ISS[5:0] = 0x05：Translation fault at level 1
  - 说明 Guest 尝试执行地址 0x3c5 处的指令，但该地址没有映射

**根本原因**：
汇编代码中存在寄存器冲突：
```asm
// 错误的代码
ldp  x10, x11, [x10, #0]  // x10 既是基址寄存器又是目标寄存器！
```

在 ARM64 中，`ldp` 指令的执行顺序可能导致：
1. 先从 `[x10]` 加载值到 x10（覆盖了原来的地址）
2. 再从 `[x10+8]` 加载值到 x11（但此时 x10 已经不是地址了！）

结果：ELR_EL2 被设置成了 SPSR_EL2 的值

**修复方法**：
```asm
// 正确的代码
ldr  x1, [x10, #0]        // elr_el2
ldr  x2, [x10, #8]        // spsr_el2
msr  elr_el2, x1
msr  spsr_el2, x2
```

使用独立的加载指令，避免寄存器冲突。

**调试手段**：
- 添加打印语句：`ax_println!("About to enter guest, ELR_EL2={:#x}, SPSR_EL2={:#x}")`
- 发现 ELR_EL2 的值异常（0x3c5 而不是预期的 0x40200000）
- 检查汇编代码中的寄存器使用模式

---

#### 问题 2: 硬编码偏移量错误

**错误现象**：
程序在进入 Guest 后卡死，没有任何输出。

**错误分析**：
在汇编代码中硬编码了结构体偏移量：
```asm
add  x9, x0, #288         // 假设 HypervisorCpuState 大小是 288
```

但通过调试输出发现实际大小是 **280 字节**：
```
[DEBUG] VmCpuRegisters size: 560
[DEBUG] HypervisorCpuState offset: 0
[DEBUG] GuestCpuState offset: 280    // 实际是 280，不是 288！
```

**根本原因**：
- 手动计算偏移量容易出错
- 结构体布局可能因编译器优化而变化
- 硬编码破坏了代码的可维护性

**修复方法**：
参考 h_1_0 (RISC-V) 的实现，使用**编译时偏移量注入**：

```rust
// vcpu.rs
global_asm!(
    include_str!("guest.S"),
    // 通过 const 参数注入偏移量
    hyp_x30 = const hyp_gpr_offset(30),
    hyp_sp = const hyp_sp_offset(),
    guest_x0 = const guest_gpr_offset(0),
    guest_sp = const guest_sp_offset(),
    guest_elr_el2 = const guest_csr_offset!(elr_el2),
    guest_spsr_el2 = const guest_csr_offset!(spsr_el2),
);
```

```asm
// guest.S - 使用占位符
str     x30, [x0, {hyp_x30}]      // {hyp_x30} 会被替换成实际偏移
ldr     x1, [x0, {guest_sp}]      // {guest_sp} 会被替换成实际偏移
```

**调试手段**：
- 使用 `offset_of!` 宏打印结构体布局：
  ```rust
  ax_println!("[DEBUG] VmCpuRegisters size: {}", size_of::<VmCpuRegisters>());
  ax_println!("[DEBUG] GuestCpuState offset: {}", offset_of!(VmCpuRegisters, guest_regs));
  ```
- 对比预期值和实际值，发现偏移量错误

---

#### 问题 3: Stage-2 页表配置与实际页表级别不匹配

**错误现象**：
```
Unhandled trap: EC=0x20, ESR_EL2=0x82000005, ELR_EL2=0x40200000
```
- EC=0x20：Instruction Abort
- IFSC=0x05：Translation fault at level 1
- Guest 入口地址 0x40200000 无法访问

**错误分析**：
查看页表结构发现创建了 **4 级页表**：
```
L0[0] = 0x0000000040437003 (table, next level PA: 0x40437000)
  L1[1] = 0x0000000040438003 (table)
    L2[1] = 0x0000000040439003 (table)    // L2 也是表项！
      L3[0] = ... (实际的页表项)
```

但 VTCR_EL2 配置为 **3 级页表**：
```rust
// 错误的配置
vtcr_el2 |= 0b01 << 6;  // SL0 = 0b01：从 Level 1 开始
vtcr_el2 |= 25;         // T0SZ = 25：39-bit 地址空间
```

**根本原因**：
- `axaddrspace` 模块创建的是 4 级页表（L0→L1→L2→L3）
- VTCR_EL2 配置为 3 级页表（L1→L2→L3）
- 配置不匹配导致 MMU 无法正确翻译地址

**修复方法**：
修改 VTCR_EL2 配置以匹配 4 级页表：
```rust
// 正确的配置
vtcr_el2 |= 0b10 << 6;  // SL0 = 0b10：从 Level 0 开始（4 级页表）
vtcr_el2 |= 16;         // T0SZ = 16：48-bit 地址空间
```

**调试手段**：
- 打印页表内容，逐级检查：
  ```rust
  unsafe {
      let l0_table = core::slice::from_raw_parts(root_va.as_ptr() as *const u64, 512);
      for i in 0..8 {
          if l0_table[i] != 0 {
              let next_level_addr = l0_table[i] & 0xFFFF_FFFF_F000;
              ax_println!("  L0[{}] = {:#018x} (next level PA: {:#x})", i, l0_table[i], next_level_addr);
              
              // 继续检查 L1 表
              let l1_va = phys_to_virt(PhysAddr::from(next_level_addr as usize));
              let l1_table = core::slice::from_raw_parts(l1_va.as_ptr() as *const u64, 512);
              // ...
          }
      }
  }
  ```
- 发现 L2 也是表项，说明实际是 4 级页表
- 对照 ARM Architecture Reference Manual，确认 SL0 的正确配置

---

#### 问题 4: 汇编代码中的寄存器保存/恢复顺序错误

**错误现象**：
程序在 `_run_guest` 调用后卡死，没有返回。

**错误分析**：
在恢复 Guest 寄存器时：
```asm
// 错误的代码
ldr     x1, [x0, {guest_sp}]        // x1 = guest sp
mov     sp, x1                       // 设置 sp
ldp     x0, x1, [x0, {guest_x0}]    // 问题：x0 被覆盖后，无法再访问结构体！
eret
```

当 `ldp x0, x1, [x0, ...]` 执行时：
1. x0 被第一个加载值覆盖
2. 失去了访问 VmCpuRegisters 的能力
3. 后续指令可能访问非法地址

**根本原因**：
寄存器被覆盖的顺序不当，导致基址寄存器在使用完之前就被破坏。

**修复方法**：
使用临时寄存器缓存值，最后再恢复：
```asm
// 正确的代码
ldr     x1, [x0, {guest_sp}]        // x1 = guest sp
ldr     x2, [x0, {guest_x0}]        // x2 = guest x0
ldr     x3, [x0, {guest_x1}]        // x3 = guest x1

// 设置 sp
mov     sp, x1

// 最后恢复 x0, x1（此时不再需要访问结构体）
mov     x0, x2
mov     x1, x3

eret
```

**调试手段**：
- **简化测试**：将 `_run_guest` 简化为只有 `ret` 指令
  ```asm
  .global _run_guest
  _run_guest:
      ret  // 立即返回，测试基本调用流程
  ```
- 发现简化版本可以正常返回，说明问题在完整实现中
- 逐步恢复代码，找出导致卡死的部分
- 检查寄存器使用模式，发现覆盖顺序问题

---

#### 问题 5: Guest 未能正常执行（最终状态）

**当前现象**：
```
[h_1_1] About to enter guest, ELR_EL2=0x40200000, SPSR_EL2=0x3c5
[h_1_1] Guest exited, returned from _run_guest
[h_1_1] VM Exit: EC=0x0, ESR_EL2=0x0, ELR_EL2=0x40200000
```

**分析**：
- EC=0 表示没有捕获到异常类型
- 程序能成功进入和退出 `_run_guest`
- 但 Guest 未能正常执行到 HVC/SMC 指令

**原因**：
当前实现是**最简化版本**，只保存/恢复了必要的寄存器（x0, sp, x30）：
```asm
_run_guest:
    str     x30, [x0, {hyp_x30}]    // 只保存 x30
    mov     x9, sp
    str     x9, [x0, {hyp_sp}]      // 只保存 sp
    
    // 恢复 guest 寄存器
    ldr     x1, [x0, {guest_elr_el2}]
    msr     elr_el2, x1
    ldr     x1, [x0, {guest_spsr_el2}]
    msr     spsr_el2, x1
    ldr     x1, [x0, {guest_sp}]
    mov     sp, x1
    ldr     x1, [x0, {guest_x0}]
    mov     x0, x1
    
    eret  // 进入 guest
```

Guest 代码需要更多寄存器才能正常运行，但我们没有恢复它们。

**后续改进方向**：
1. 恢复所有 Guest 通用寄存器（x0-x30, sp）
2. 正确处理 Guest 退出时的异常信息
3. 完善 VM Exit 处理流程

---

### 调试技巧总结

#### 1. 添加调试日志

**结构体布局调试**：
```rust
use core::mem::{size_of, offset_of};
ax_println!("[DEBUG] VmCpuRegisters size: {}", size_of::<VmCpuRegisters>());
ax_println!("[DEBUG] HypervisorCpuState offset: {}", offset_of!(VmCpuRegisters, hyp_regs));
ax_println!("[DEBUG] GuestCpuState offset: {}", offset_of!(VmCpuRegisters, guest_regs));
```

**关键寄存器状态**：
```rust
ax_println!("[h_1_1] About to enter guest, ELR_EL2={:#x}, SPSR_EL2={:#x}", 
    ctx.guest_regs.elr_el2, ctx.guest_regs.spsr_el2);
ax_println!("[h_1_1] Guest exited, returned from _run_guest");
```

**页表内容检查**：
```rust
unsafe {
    let l0_table = core::slice::from_raw_parts(root_va.as_ptr() as *const u64, 512);
    for i in 0..8 {
        if l0_table[i] != 0 {
            ax_println!("  L0[{}] = {:#018x}", i, l0_table[i]);
        }
    }
}
```

**异常信息解析**：
```rust
let esr_el2 = SYSREG.esr_el2.read();
let ec = (esr_el2 >> 26) & 0x3F;
ax_println!("[h_1_1] VM Exit: EC={:#x}, ESR_EL2={:#x}, ELR_EL2={:#x}", 
    ec, esr_el2, ctx.guest_regs.elr_el2);
```

#### 2. 使用 QEMU 日志

QEMU 启动参数中已经包含了详细的日志选项：
```bash
qemu-system-aarch64 ... \
  -D qemu.log \
  -d in_asm,int,mmu,pcall,cpu_reset,guest_errors
```

日志类型说明：
- `in_asm`：反汇编执行的指令
- `int`：中断和异常
- `mmu`：内存管理单元（页表翻译）
- `pcall`：特权调用（如 HVC/SMC）
- `cpu_reset`：CPU 复位事件
- `guest_errors`：Guest 错误

查看日志：
```bash
tail -100 qemu.log  # 查看最后 100 行
grep "0x40200000" qemu.log  # 搜索特定地址
grep "Exception" qemu.log   # 搜索异常信息
```

#### 3. 渐进式调试

**策略**：从最简单的实现开始，逐步增加功能

**步骤 1**：测试基本调用流程
```asm
.global _run_guest
_run_guest:
    ret  // 立即返回，验证 Rust 代码能正确调用汇编
```

**步骤 2**：测试寄存器保存/恢复
```asm
_run_guest:
    str     x30, [x0, {hyp_x30}]  // 保存返回地址
    ldr     x30, [x0, {hyp_x30}]  // 恢复返回地址
    ret
```

**步骤 3**：测试进入 Guest
```asm
_run_guest:
    str     x30, [x0, {hyp_x30}]
    // ... 设置 elr_el2, spsr_el2
    eret  // 进入 guest
```

**步骤 4**：完善异常处理
```asm
_guest_exit:
    // ... 异常向量表
.Lguest_exit_sync:
    // ... 保存 guest 状态
    // ... 恢复 hypervisor 状态
    ret
```

#### 4. 对比参考实现

参考同项目中其他架构的实现：
- **h_1_0** (RISC-V)：学习编译时偏移量注入
- **arm_vcpu**：学习 ARM64 的宏定义模式

关键借鉴点：
```rust
// h_1_0 的占位符模式
global_asm!(
    include_str!("guest.S"),
    hyp_ra = const hyp_gpr_offset(GprIndex::RA),
    guest_sp = const guest_gpr_offset(GprIndex::SP),
    // ...
);
```

```asm
// h_1_0 的汇编代码
sd   ra, ({hyp_ra})(a0)    // {hyp_ra} 会被替换成实际偏移
ld   sp, ({guest_sp})(a0)  // {guest_sp} 会被替换成实际偏移
```

#### 5. 错误码解析

**ESR_EL2 (Exception Syndrome Register) 结构**：
```
Bits [31:26] - EC (Exception Class)
Bits [25]    - IL (Instruction Length)
Bits [24:0]  - ISS (Instruction Specific Syndrome)
```

**常见 EC 值**：
- `0x00`：Unknown/Uncategorized
- `0x20`：Instruction Abort from Lower EL
- `0x24`：Data Abort from Lower EL  
- `0x16`：HVC instruction (EL2)
- `0x17`：SMC instruction (EL3)

**IFSC/DFSC (Fault Status Code) - ISS[5:0]**：
- `0x04`：Translation fault, level 0
- `0x05`：Translation fault, level 1
- `0x06`：Translation fault, level 2
- `0x07`：Translation fault, level 3

**示例分析**：
```
ESR_EL2 = 0x82000005
  EC = 0x20 (Instruction Abort from Lower EL)
  ISS = 0x2000005
    IFSC = 0x05 (Translation fault, level 1)
```
→ Guest 尝试执行的地址在 Stage-2 页表的 Level 1 无法翻译

#### 6. 编译器辅助调试

Rust 编译器会报告未使用的参数：
```
error: multiple unused asm arguments
   --> src/vcpu.rs:68:5
    |
68  |     hyp_x0 = const hyp_gpr_offset(0),
    |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ named argument never used
```

---

### 最终实现状态

**成功完成**：
-  正确配置 EL2 环境
-  设置 Stage-2 页表（4 级页表，VTCR_EL2 匹配）
-  实现 Guest 上下文切换（最简版本）
-  程序能成功进入 Guest 并返回

**当前限制**：
- 只恢复了最基本的寄存器（x0, sp, x30）
- Guest 无法正常执行完整代码
- EC=0 表示未捕获到预期的 HVC/SMC 异常

**后续改进方向**：
1. 恢复所有 Guest 通用寄存器（x0-x30）
2. 完善异常向量表处理
3. 正确处理 Guest VM Exit
4. 实现完整的 PSCI 调用支持

---
