# h_1_1、h_2_1、h_3_1、h_4_1 系列实验演进总结

## 实验演进概述

h_1_1到h_4_1系列实验是ArceOS中关于AArch64架构虚拟化的逐步演进，每个实验都在前一个实验的基础上增加新的虚拟化功能。

### h_1_1：基础虚拟化支持

h_1_1实验建立了AArch64架构下的基础虚拟化框架：

1. **虚拟化初始化**：设置Hypervisor模式，配置HCR_EL2寄存器启用虚拟化
2. **异常处理**：实现异常向量表，处理从客户机（Guest）到Hypervisor的异常返回
3. **PSCI调用处理**：处理客户机发出的PSCI（Power State Coordination Interface）系统调用，如关机、重启等
4. **基础寄存器保存/恢复**：在虚拟化进入和退出时保存和恢复必要的CPU寄存器

### h_2_1：内存虚拟化基础

h_2_1在h_1_1基础上增加了内存虚拟化功能：

1. **地址空间管理**：引入虚拟机的地址空间管理，设置客户机的内存映射
2. **设备内存映射**：将设备内存区域映射到客户机地址空间，支持客户机访问硬件设备
3. **物理内存分配**：为客户机分配物理内存区域并建立相应的映射关系

### h_3_1：虚拟中断系统

h_3_1实现了虚拟中断系统：

1. **虚拟中断控制器**：实现对虚拟中断的管理和分发
2. **中断虚拟化**：将物理中断虚拟化后传递给客户机
3. **中断状态管理**：维护虚拟中断的状态，确保中断处理的正确性

### h_4_1：Stage-2页表和内存管理（已修改 + 补充本次 Debug 涉及的硬件行为）

h_4_1在前序实验能进 Guest、能处理基本 trap的基础上，进一步支持了一个内核客户机文件成功起用户态程序，用户态程序执行一个退出关机的命令经过最终guest 发 PSCI SYSTEM_OFF（SMC64）到hypervisor 捕获并解析最终由hypervisor 执行关机

1. **Stage-2页表设置（VTTBR_EL2 + VTCR_EL2）**

   * 通过配置 **VTTBR_EL2** 指向 Stage-2 页表基址，并结合（实验实现对应的）Stage-2 控制参数，启用并管理 **IPA→PA** 的二阶段转换。
   * 在开启 HCR_EL2.VM 后，Guest 每一次访存会经历：

     * Stage-1：Guest 自己的 VA→IPA（由 TTBRx_EL1/TCR_EL1 等决定）
     * Stage-2：Hypervisor 的 IPA→PA（由 VTTBR_EL2 指向的 Stage-2 页表决定）
   * 这使 Hypervisor 能够限制 Guest 只能访问被允许的物理内存范围，并能对不同内存段设置不同权限（可读/可写/可执行、设备属性等）。

2. **访存异常分类与故障来源判定**
   * 通过 **ESR_EL2.EC** 判断异常大类：例如 Data Abort from a lower EL（Guest 引发的数据访问异常）。
   * 通过 **FAR_EL2** 和 **HPFAR_EL2** 辅助区分错误来源：

     * **FAR_EL2**：记录导致异常的故障虚拟地址（对齐后/或用于定位 VA 侧的访问）。
     * **HPFAR_EL2**：当异常与 Stage-2 翻译相关时，可提供与 IPA 相关的额外信息（用于定位二阶段转换/权限问题）。
   * 结合 **ISS** 字段中的 **WnR**（读/写）与 **DFSC**（Data Fault Status Code）进一步区分翻译缺失、权限不足、对齐/访问标志问题等。
     * **HPFAR_EL2 为空/无有效信息** 往往问题可能不在 Stage-2，而可能来自 Guest 自己的 Stage-1（例如 Guest 访问了非法 VA，或出现了空指针 + offset）。
     * **FAR_EL2=0x10 这类极小地址** 常见于空指针 + 结构体偏移的访问形态。

3. **页错误处理与动态映射（按需建立 Stage-2 映射）**

   * 当 Guest 访问尚未映射或权限不符的地址时，硬件会触发异常并切换到 EL2，由 Hypervisor 接管处理。
   * 目前最终版的h_4_1 在功能上支持：

     * 在异常处理路径中，读取异常信息（ESR_EL2/FAR_EL2/HPFAR_EL2），定位访问目标与访问类型；
     * 更新 Stage-2 页表项并进行必要的 TLB 维护，从而让 Guest 能继续执行。


---

## AArch64架构虚拟化过程中硬件行为详细阐述

### 1. 异常级别和虚拟化架构

AArch64架构定义了4个异常级别（EL0-EL3），在虚拟化中：

* **EL2**：通常用作Hypervisor运行级别
* **EL1**：客户机操作系统运行级别
* **EL0**：客户机应用程序运行级别

### 2. 关键系统寄存器行为

* **HCR_EL2（Hypervisor Configuration Register）**：

  * `VM`位：启用Stage-2地址转换
  * `RW`位：设置客户机执行AArch64还是AArch32
  * `TGE`位：启用通用定时器虚拟化

* **VTTBR_EL2（Virtualization Translation Table Base Register）**：

  * 存储Stage-2页表的基地址
  * 用于客户机虚拟地址到物理地址的转换（Stage-2 的 IPA→PA）

* **ELR_EL2（Exception Link Register）**：

  * 存储客户机发生异常时的返回地址（用于返回 Guest 继续执行）

* **SPSR_EL2（Saved Program Status Register）**：

  * 保存客户机异常发生时的处理器状态（异常返回时恢复）

> **（Debug 补充）ESR_EL2 / FAR_EL2 / HPFAR_EL2**

* **ESR_EL2（Exception Syndrome Register）**：

  * `EC` 指示异常类型（例如 Data Abort from lower EL）
  * `ISS` 给出具体原因细节（包括 WnR、DFSC 等）
* **FAR_EL2（Fault Address Register）**：

  * 保存导致异常的故障虚拟地址（常用于快速判断是否是空指针 + 偏移）
* **HPFAR_EL2（Hypervisor IPA Fault Address Register）**：

  * 在与 Stage-2 翻译相关的异常中提供额外信息，用于定位 IPA 侧的故障来源
  * Debug 时常用它来区分Stage-1 问题还是Stage-2 问题

### 3. 地址转换机制

AArch64虚拟化采用两阶段地址转换：

* **Stage 1**：虚拟地址（VA）到中间物理地址（IPA）
* **Stage 2**：中间物理地址（IPA）到物理地址（PA）

客户机看到的是VA到IPA的转换，由客户机页表控制；Hypervisor控制IPA到PA的转换，即Stage-2页表。

### 4. 异常处理机制

* 当客户机执行HVC或SMC指令时，硬件会：

  1. 保存客户机的执行状态到SPSR_EL2
  2. 保存异常返回地址到ELR_EL2
  3. 切换到EL2并跳转到异常向量表
  4. 执行Hypervisor的异常处理程序

> **（Debug 补充）当 Guest 触发 Data Abort（访存异常）时**

* 同步异常同样会保存现场到 EL2，并由 EL2 异常向量接管
* Hypervisor 可读取 ESR_EL2/FAR_EL2/HPFAR_EL2 判定：

  * 是 Stage-1 引发的非法 VA/权限问题
  * 还是 Stage-2 引发的 IPA→PA 翻译缺失/权限拒绝
* 再决定补页、修改映射、或者终止 Guest

### 5. 内存管理单元（MMU）行为

* **TLB（Translation Lookaside Buffer）**：硬件维护虚拟地址到物理地址的缓存
* **Stage-2页表**：Hypervisor设置，控制客户机内存访问权限
* **内存访问权限检查**：硬件同时检查Stage-1和Stage-2的权限

> **（Debug 补充）TLB 与页表更新可见性**

* 修改页表项后若缺少必要的 TLB invalidate 与屏障同步，可能导致 Guest 继续使用旧转换结果
* 因此在动态映射/补页路径中需特别关注 TLB 与屏障

### 6. 寄存器访问虚拟化

* 某些系统寄存器访问会被截获并由Hypervisor处理
* 使用HCR_EL2的TRAP位来控制哪些寄存器访问需要被截获

> **（Debug 补充）Guest system register 上下文一致性**

* VMExit/VMEntry 并不会自动保存/恢复 Guest 的所有 system registers
* 当 Guest 依赖 thread pointer、用户栈等寄存器维持运行时状态时，Hypervisor 必须显式管理其保存/恢复，否则可能引发运行到用户态/系统调用后突然崩溃的现象

### 7. 时钟和定时器虚拟化

* **CNTVO_EL2**：虚拟化定时器偏移寄存器
* 硬件基于物理定时器和虚拟偏移计算虚拟定时器值

### 8. 虚拟中断系统

* **VGIC（Virtual Generic Interrupt Controller）**：硬件辅助的虚拟中断控制器
* **ICH_*寄存器**：管理虚拟中断的激活和挂起状态

